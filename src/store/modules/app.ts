/* 此app是控制左侧菜单栏展开和关闭的，因为在手机端是默认关闭所以起名为app */

import { getSidebarSatus, setSidebarStatus } from "@/utils/cache/localStorage";
import { defineStore } from "pinia";


export const useApp = defineStore('app',()=>{
  
})